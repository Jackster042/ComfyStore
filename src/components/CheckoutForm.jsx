import React from "react";
import { Form } from "react-router-dom";

const CheckoutForm = () => {
  return (
    <section>
      <h1>Checkout Form</h1>
    </section>
  );
};

export default CheckoutForm;
